import React from 'react';

import MenuCategory from './components/MenuCategory';

const Components = () => {
  return (
    <MenuCategory category="Components">
      {/* Add Components here for easy access */}
    </MenuCategory>
  );
};

export default Components;
